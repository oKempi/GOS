package Utilities;

import java.io.File;
import java.io.IOException;
import java.util.Scanner;

public class FileManager {//will be used to save and load data from files (choices.txt)

    public static String[] read(String filename) {
        if(filename.equals("situations")) {
            try{
                File file = new File("situations.txt");

            }
        }
    }
}